services:
  postgres:
    image: postgres:17-alpine
    container_name: tt4d-postgres
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASS}
      - POSTGRES_DB=${POSTGRES_NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - tt4d-network

networks:
  tt4d-network:
    driver: bridge

volumes:
  postgres_data:
